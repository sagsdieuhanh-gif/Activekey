# TRUNGKIEN ADAS V2.3.1 — MENU FIX2

## Nội dung sửa
- Sửa lỗi build Kotlin tại `AdasMenuDialog.kt`.
- Thay `ScrollView.LayoutParams(...)` bằng `ViewGroup.LayoutParams(...)`.
- Không thay đổi Smart Lead, FCW, HMW, LDW, TLC, TTS, trial, device code hay DG12.
- Package: `com.trungkien.adas.v231menu`.
- Landscape giữ nguyên theo cấu hình hiện tại.

## Lưu ý
Các `LinearLayout.LayoutParams(...)` khác là hợp lệ và được giữ nguyên.
